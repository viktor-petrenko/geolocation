package com.geolocation.base;

import com.geolocation.utils.JarRunner;

public class BaseTest {
    public static JarRunner jarRunner = new JarRunner();

    public String invalidZip = "00000"; // enum might be needed later or think about data usage

}
